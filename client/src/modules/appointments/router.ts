import appointment from './views/appointment.vue';

const moduleRoute = [
    {
        path: '/appointments',
        name: 'Appointment',
        component: appointment
    }
]

export default moduleRoute;